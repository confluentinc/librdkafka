version: v1.0
name: share-consumer-metric-e2e-test

agent:
  machine:
    type: s1-prod-ubuntu24-04-amd64-1

execution_time_limit:
  hours: 1

global_job_config:
  prologue:
    commands:
      - checkout
      - '[[ -z "$GIT_REF" ]] || git checkout $GIT_REF'
      - sem-version java 17
      - sudo apt update
      - sudo apt install -y wget docker-compose-plugin
  env_vars:
    - name: KAFKA_VERSION
      value: '4.2.0'
    - name: CACHE_TAG
      value: '1'

blocks:
  - name: 'Share consumer metric e2e test'
    dependencies: []
    task:
      jobs:
        - name: 'Verify consumer.share.* metrics in topic'
          commands:
            - cache restore share-telemetry-${KAFKA_VERSION}-${CACHE_TAG}
            - ./packaging/tools/setup-share-telemetry-testing.sh
            - cache store share-telemetry-${KAFKA_VERSION}-${CACHE_TAG} tests/share_telemetry_verify/.work
